<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coffeeforcodeapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_coffee"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoffeeForCodeApp">
        <activity android:name=".Todos_Os_produtosActivity"></activity>
        <activity android:name=".SaibaMaisActivity" />
        <activity android:name=".Ver_e_Deletar_CartaoActivity" />
        <activity android:name=".CadastrarenderecoActivity" />
        <activity android:name=".CarrinhoDeCompraActivity" />
        <activity android:name=".CadastrarCartoesActivity" />
        <activity android:name=".CartoesActivity" />
        <activity android:name=".SejaParceiroActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".CriarContaActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>